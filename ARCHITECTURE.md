multiplayer-fps/
├── Cargo.toml          (workspace racine)
├── server/
│   ├── Cargo.toml
│   └── src/
│       └── main.rs     (code du serveur)
└── client/
    ├── Cargo.toml
    └── src/
        └── main.rs     (code du client)